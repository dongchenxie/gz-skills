---
name: gzcots-news
description: 广建职校公众号"学校新闻"类推文生成（半自动）。当用户要写学校承办竞赛、与公办单位共建、人员获奖、慈善慰问等机构新闻时使用。
---

# gzcots-news

**类型**：学校新闻
**触发感**：建立机构权威（"我们和谁合作"、"我们办了什么大事"）
**自动化程度**：**半自动**。来源杂、信息靠口述、老板有自己的行文风格 + 会多次修改。
本 skill 只做信息整理 + 骨架占位，**正文留给老板**。

## 启动前置检查

同 `gzcots-publish/SKILL.md`。

## 输入：6 道必问

**Q1** 客户画像（学校新闻类**不强问**营销画像，但要确认基调）：
- A. **对外宣传**（面向潜在 B 端 / C 端客户，体现实力）
- B. **行业关系**（面向同行 / 协会单位，体现资源）
- C. **内部喜讯**（面向已有学员 / 合作方）

**Q2** 活动 / 事件主题（开放问答，1 句话）。

**Q3** 主办 / 承办 / 协办单位（列全，每家的全称必须准确 —— 错一个字老板会发火）。

**Q4** 参与人员 / 获奖人员（姓名 + 职务 + 单位）。

**Q5** 事件时间、地点。

**Q6** 老板特别交代要突出的点（开放问答）。如老板有原话，原话照填，**不要润色**。

## 抓取

学校新闻类**不自动抓取**外部站点（来源是主办单位的内部通知 / 老板口述，公开网上一般查不到）。
如用户主动给了 URL（例如某政府单位发的活动新闻通稿），可用 `WebFetch` 抓一次作为素材引用，**但不作为正文主体**。

## 撰稿规则

- **不要替老板拿主意**。skill 只生成"占位骨架"，正文 60% 以上是空白引导字段。
- 句式倾向官方 / 正式（"在 XXX 单位主办、XXX 单位承办、XXX 单位协办的 XXX 大会上"），不要营销腔。
- 单位名称、人名、职务、奖项名称这些字段必须从 Q3/Q4 原样填入，不允许 skill 自己脑补。
- 老板交代要突出的点（Q6）以 ⑤ 重点卡片块单独成段。

## 生成的"半成品"

按 `_shared/135-editor.md` 骨架渲染，但正文段落用占位符标出**待老板补写**：

```html
<section style="...">
  <p style="margin:0 0 14px;">
    [待补：开场，背景介绍 1-2 句]
  </p>
</section>
<section style="...">
  <p style="margin:0 0 14px;">
    [待补：活动主体过程，1-2 段]
  </p>
</section>
```

必带块：①②③④⑨。`⑤` 重点卡片用于 Q6 的强调点。**不强加 CTA / B 端引流**（学校新闻不是营销稿）。

## 审核 / 输出

简化版审核（只跑维度 1 错字 + 维度 2 排版）。

输出双产物：
- `article.html`（半成品骨架）
- `fill-in.md`（待补段落清单 + 老板需要决定的事项列表，便于打印或群里抄送）
